<section class="section">
  <div class="page-width page-width--narrow text-center">
    <h1>{{ 'general.404.title' | t }}</h1>
    <p>{{ 'general.404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="btn" style="width:auto">{{ 'general.404.link' | t }}</a>
  </div>
</section>

{% schema %}
{ "name": "404" }
{% endschema %}
